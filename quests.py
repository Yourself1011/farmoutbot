quests = {"neighbour": {}}


async def quests(message, client):
    return
    # isn't this basically contracts kinda maybe sorta but didn't you delete those? n0 they're temporarily gone
    # i wanna imrpvoe them eventually
    # but i just got them out of the way to work on other things first
    # also is there a way to delete all buttons on msgs when bot starts no since we don't store them
